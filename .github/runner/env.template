# Copy this file to ~/actions-runner/.env after running setup.sh
#
# Pre-job hook: health check GPU/Docker/disk before each job
ACTIONS_RUNNER_HOOK_JOB_STARTED=/home/ethan/SDR_OS/.github/runner/pre-run.sh

# Limit concurrent jobs (RAM is tight at 8 GB)
# Only 1 job at a time to avoid OOM
RUNNER_CONCURRENCY=1

# Docker cleanup: prune dangling images after each job
# (uncomment if disk fills up from CI builds)
# ACTIONS_RUNNER_HOOK_JOB_COMPLETED=/home/ethan/SDR_OS/.github/runner/post-run.sh
