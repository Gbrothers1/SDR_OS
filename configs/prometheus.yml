# SDR_OS Prometheus configuration

global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  # Transport server metrics (Phase 2)
  # - job_name: 'transport-server'
  #   static_configs:
  #     - targets: ['transport-server:9100']

  - job_name: 'nats'
    static_configs:
      - targets: ['nats:8222']
    metrics_path: /varz
